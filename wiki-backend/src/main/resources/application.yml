# server setting
server:
  port: 8080
  tomcat:
    threads:
      max: 100


# profile setting
# application-{spring:profiles:active}.yml

---
spring:
  profiles:
    active:
      - local # 활성화할 기본 프로파일 설정 

---
spring:
  profiles: local

---
spring:
  profiles: dev

---
spring:
  profiles: prod